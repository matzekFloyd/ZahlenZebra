/** Canvas / probe geometry (matches Processing sketch). */
export const VIEW_W = 640;
export const VIEW_H = 480;
export const PROBE_X = 300;
export const PROBE_Y = 300;
export const PROBE_W = 2;
export const PROBE_H = 2;

/** Same tolerance as `DigitColorModel` in the Java sketch. */
export const MATCH_TOLERANCE = 50;

export const STORAGE_KEY = "zahlenzebra-calibration-v1";
